# Local preview scratch
_preview.html

# The original photograph stays local. Only the prepped grayscale bust is
# committed, since that is what the ASCII is generated from and what the
# README already shows. Drop your own source-photo.jpg in before running
# `make photo`.
source-photo.jpg

# Python
__pycache__/
*.pyc
